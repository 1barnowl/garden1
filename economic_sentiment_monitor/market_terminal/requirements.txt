# ── core runtime ──────────────────────────────────────────────────────────────
textual>=0.52.0
feedparser>=6.0
requests>=2.31
yfinance>=0.2.36
vaderSentiment>=3.3.2
pyyaml>=6.0

# ── optional NLP upgrades ─────────────────────────────────────────────────────
# Uncomment to enable FinBERT (accurate, slow, needs ~2 GB RAM)
# transformers>=4.38
# torch>=2.2

# ── optional full-text ingestion ──────────────────────────────────────────────
# Uncomment to enable full article body fetching
# newspaper3k>=0.2.8

# ── optional NER (entity extraction upgrade) ──────────────────────────────────
# Uncomment then: python -m spacy download en_core_web_sm
# spacy>=3.7

# ── future: async API layer ───────────────────────────────────────────────────
# fastapi>=0.110
# uvicorn[standard]>=0.29
# httpx>=0.27
# asyncpg>=0.29
# sqlalchemy[asyncio]>=2.0
# alembic>=1.13
# psycopg[binary]>=3.1
# redis>=5.0
# pydantic>=2.6
# pydantic-settings>=2.2

# ── future: analytics & charting ──────────────────────────────────────────────
# pandas>=2.2
# plotly>=5.20
# duckdb>=0.10

# ── future: observability ─────────────────────────────────────────────────────
# prometheus-client>=0.20
# opentelemetry-api>=1.24
# opentelemetry-sdk>=1.24

# ── dev & test ────────────────────────────────────────────────────────────────
# pytest>=8.1
# pytest-asyncio>=0.23
